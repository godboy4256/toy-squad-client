import styled from 'styled-components';

export const ApplicationListContainer = styled.div`
  width: fit-content;
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: white;
  border-radius: 30px;
  padding: 30px;
`;

export const ApplicationListTitle = styled.h3`
  font-size: 1.7rem;
  white-space: nowrap;
  text-align: center;
  font-weight: 700;
  margin-bottom: 30px;
`;

export const ApplicationListBody = styled.div``;
